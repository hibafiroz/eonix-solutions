> Form handling basics:

Form handling means managing user input using React state and events.
It is done using controlled components, where input values are controled by state and updated using onChange.
we use onSubmit to submit form and use preventDefault() to prevent page reload.

In normal HTML forms, when we submit a form, the page reloads automatically and the data is sent to the server.
But in React, we usually stop the page reload and handle everything using JavaScript.


function App() {
  const [name, setName] = useState("");

  function handleSubmit(e) {
    e.preventDefault();                 //This stops the browser’s default form behavior
    console.log(name);
  }
  return (
    <form onSubmit={handleSubmit}>

      <input
        type="text"
        value={name}
        onChange={(e) => setName(e.target.value)}
      />
      <button type="submit">Submit</button>
    </form>
  );
}


> onChange Event:
The onChange event runs whenever the user changes the input value.

onChange={(e) => setName(e.target.value)}
e = event object
e.target = the element that triggered event
e.target.value = current value of input
So React reads the user input and updates state.


> Form Submission:
Forms have a submit event called onSubmit.
<form onSubmit={handleSubmit}>
When user clicks submit button, the onSubmit event fires.

function handleSubmit(e) {
  e.preventDefault();
  console.log("Form Submitted");
}


> Handling Multiple Inputs:

Real forms usually have many inputs like: name, email, password
Instead of creating many states like this: name state, email state, password state
we usually keep one state object.

> Example:

function Form() {
  const [formData, setFormData] = useState({
    name: "",
    email: "",
    password: ""
  });

  function handleChange(e) {

    const { name, value } = e.target;

    setFormData({
      ...formData,
      [name]: value
    });

  }

  return (
    <form>

      <input
        name="name"
        value={formData.name}
        onChange={handleChange}
      />

      <input
        name="email"
        value={formData.email}
        onChange={handleChange}
      />

      <input
        name="password"
        value={formData.password}
        onChange={handleChange}
      />

    </form>
  );
}

name attribute identifies input
[value] gets value from state
This updates the correct field dynamically.


> Handling Different Input Types:

Forms contain many types of inputs.
React handles each slightly differently.

1. Text Input:

<input
  type="text"
  value={name}
  onChange={(e) => setName(e.target.value)}
/>


2. Textarea:

<textarea
  value={message}
  onChange={(e) => setMessage(e.target.value)}
></textarea>


3. Select Dropdown:

<select
  value={country}
  onChange={(e) => setCountry(e.target.value)}
>

  <option value="India">India</option>
  <option value="USA">USA</option>

</select>


4. Checkbox:

Checkbox uses checked property.

Example:
const [isChecked, setIsChecked] = useState(false);

<input
  type="checkbox"
  checked={isChecked}
  onChange={(e) => setIsChecked(e.target.checked)}
/>

e.target.checked → true or false


5. Radio Buttons:
Radio buttons represent one selected option from many.

Example:
const [gender, setGender] = useState("");

<input
  type="radio"
  value="male"
  checked={gender === "male"}
  onChange={(e) => setGender(e.target.value)}
/>

<input
  type="radio"
  value="female"
  checked={gender === "female"}
  onChange={(e) => setGender(e.target.value)}
/>


6. Resetting a Form:
Since inputs are controlled by state, resetting is easy.

Example:
setFormData({
  name: "",
  email: "",
  password: ""
});
All inputs become empty.


> Basic Form Validation:
React can validate inputs before submitting.

Example:
function handleSubmit(e) {
  e.preventDefault();

  if(name === ""){
    alert("Name is required");
    return;
  }

  console.log(name);
}

> Validation types:
Required fields, Email format, Password length, Number range, Pattern matching
